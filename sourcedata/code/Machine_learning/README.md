# Demo to show how to run the machine learning method

For CSP+SVM

1. Process the preprocessed data through "transform.py" to ensure it conforms to the format required for running the code.
2. Enter the path from step 1 into ".\code\Maachine_learning\CSP\main.py" 
3. Afterwards, you can run main.py.

